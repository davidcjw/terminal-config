# Session
alias t='tmux'
alias tn='tmux new -s'              # session name
alias td='tmux detach'
alias ta='tmux attach -t'           # session name
alias tk='tmux kill-session -t'     # session_name
alias tr='tmux rename-session -t'   # prev_name new_name
